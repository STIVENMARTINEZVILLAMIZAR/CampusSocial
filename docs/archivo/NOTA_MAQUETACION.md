# Ubicación del frontend

La app React (**maquetación operativa**) está en la **raíz del repositorio**:

`CampusSocial/maquetacion/`

Si todavía existe la carpeta **`agente_inteligente/maquetacion/`**, es un duplicado residual: cierra procesos que la usen (Cursor, `npm run dev`, antivirus) y elimínala manualmente para no confundir rutas.
